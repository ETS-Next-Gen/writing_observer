/* eslint-disable no-magic-numbers */

// This is just here so we don't break the build for the arrow component.
//
// The arrow component (and the helperlib) might be moved elsewhere,
// so that a dash component for the arrow isn't generated, or the arrow
// component should be extended to work independently.

const testData = {
  source: 'start',
  target: 'end'
};

export default testData;
